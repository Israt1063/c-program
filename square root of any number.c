#include<stdio.h>
#include<math.h>
int main(){

    double num,result;
    // Input from user
    printf("enter any number:");
    scanf("%lf",&num);
    //calculation
    result=sqrt(num);
    printf("%lf",result);
}